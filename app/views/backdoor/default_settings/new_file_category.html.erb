<% form_for :file_category, @file_category, :url => { :action => "add_file_category" } do |f| %>
<fieldset>

  <%= display_flash(flash) %>
  
  <p>
    <label>Category Name</label>
    <%= f.text_field :name %>
  </p>
  
  <p class="submit">
    <%= submit_tag "Add New Default Message Category" %>
    or <%= link_to "Cancel", backdoor_default_settings_path(:file_categories) %>
  </p>

</fieldset>
<% end %>